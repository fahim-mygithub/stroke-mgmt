import { colors } from '@/view/theme/colors';
import { spaces } from '@/view/theme/spaces';
import { fonts, fontFamily } from '@/view/theme/fonts';
import { elevations } from '@/view/theme/elevations';
import { breakpoints } from '@/view/theme/breakpoints';
import { radii } from '@/view/theme/radii';

export const theme = {
  colors,
  spaces,
  fonts,
  fontFamily,
  elevations,
  breakpoints,
  radii,
};
